/**
 * This would usually come from .env file
 * but since it is not sensitive information
 * and including .env in the repo is a bad practice
 * ended up just hardcoding it.
 */
const config = {
  owner: "soymvillalobos",
  repo: "turbo-octo-lamp",
}

export default config
